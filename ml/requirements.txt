# ML pipeline for predictive auto-scaling — Python 3.10+
# Core (required for the offline synthetic demo)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.28

# Prometheus collection (optional; plain requests also works)
prometheus-api-client>=0.5

# Models (heavy; install as needed)
xgboost>=1.7
tensorflow>=2.13     # provides keras; use tensorflow-macos on Apple Silicon
prophet>=1.1

# Serving
fastapi>=0.100
uvicorn>=0.23
# flask>=2.3          # alternative to fastapi (serve.py supports both)

# Notebook
jupyter>=1.0
